===============================================================================
                SNIPEJOB — CRITICAL FIX & LAUNCH GUIDE
        Everything found in the audit, everything you need to do,
                  and how to verify each fix actually worked.
===============================================================================

READ THIS FIRST
-------------------------------------------------------------------------------
Your project has two "copies" of the backend:
  1. /worker.js                       (root folder — 22 routes, fully built)
  2. /my-sniper-worker/src/index.js   (deployed file — only 3 routes)

Your live site (index.html) points at the worker deployed FROM
my-sniper-worker/, which only contains 3 of your 22 API routes. Every other
feature — the Pro upgrade flow, withdrawals, AI Apply, AI resume, KYC,
interview prep, sector trends, side tasks, the lot — has been returning
"Not Found" in production. This guide's #1 job is fixing that, then closing
two real money-fraud holes that were found, then wiring up 13 frontend
buttons that call JavaScript functions that don't exist.

Work through the steps IN ORDER. Each step has a "✅ VERIFY" block — do not
skip these. They're how you confirm the fix actually worked before moving on,
the same way the audit found these bugs in the first place.

Estimated total time: 60–90 minutes if you have your Supabase and Cloudflare
dashboards open and ready.


===============================================================================
STEP 0 — BACK UP WHAT YOU HAVE
===============================================================================
Before changing anything:

1. In your project folder, run:
     git add .
     git commit -m "checkpoint before critical fixes"
     git push origin main

   This gives you a restore point if anything goes wrong.

2. In Supabase Dashboard > SQL Editor, run:
     SELECT * FROM public.profiles LIMIT 5;
   Just to confirm you can see your current data before you touch the schema.

✅ VERIFY: You see your commit in GitHub, and the SELECT above returns rows
(or an empty result if you have no test users yet — that's fine too).


===============================================================================
STEP 1 — MERGE THE BACKEND FILES (the #1 critical fix)
===============================================================================
You're going to make my-sniper-worker/src/index.js the ONE true backend file,
fully replacing its current 3-route content with the complete 22-route
version from worker.js, PLUS the security/correctness patches below.

1. Open /worker.js (root folder) in your editor.
2. Apply EVERY code snippet given to you in the chat response under
   "WORKER.JS PATCHES" — there are 5 of them:
     (a) Hardened payment webhook + new Stripe checkout route
     (b) Postback shared-secret fix
     (c) Atomic withdrawal fix
     (d) Sector trends self-healing fix
     (e) Verify-ID response fix
   Apply them directly into worker.js, in the matching location (each
   snippet tells you which existing block it replaces or where it's
   inserted).
3. Once all 5 patches are applied to worker.js, COPY THE ENTIRE FILE
   CONTENTS of worker.js and PASTE them into
   /my-sniper-worker/src/index.js, completely replacing what's there.
4. Delete (or rename to worker.js.OLD) the root-level files that are no
   longer needed, to stop this exact bug from happening again:
     - /worker.js              (its content now lives in my-sniper-worker/src/index.js)
     - /wrangler.toml          (conflicting config — my-sniper-worker/wrangler.jsonc
                                 is the only one that should exist from now on)

   From now on: there is exactly ONE backend file
   (my-sniper-worker/src/index.js) and exactly ONE wrangler config
   (my-sniper-worker/wrangler.jsonc). Never let a second copy of either
   exist again — that duplication is exactly how this bug happened.

5. Deploy it:
     cd my-sniper-worker
     npx wrangler deploy

✅ VERIFY:
   a) Visit https://my-sniper-worker.626gl1ch.workers.dev/debug/env
      You should see {"hasSupabaseUrl":true,"hasSupabaseAnonKey":true,
      "hasSupabaseServiceRoleKey":true,"hasGeminiApiKey":true}
      (If any say false, see Step 2 below before continuing.)
   b) Visit https://my-sniper-worker.626gl1ch.workers.dev/api/jobs
      You should get {"error":"Unauthorized"} — NOT "Not Found". Getting
      "Unauthorized" is correct and means the route now exists.
   c) Run this in your terminal (replace with any path that was 404 before):
        curl -s https://my-sniper-worker.626gl1ch.workers.dev/api/trends
      You should get "Unauthorized", not "Not Found".
   d) Open Cloudflare Dashboard > Workers & Pages > my-sniper-worker >
      Settings > Bindings, and just visually confirm only ONE worker named
      "my-sniper-worker" exists (no leftover "job-finder-saas-web-app"
      worker from the old root wrangler.toml). If you see a second worker
      with that name, delete it from the dashboard to avoid confusion.


===============================================================================
STEP 2 — CLOUDFLARE WORKER SECRETS (including ONE new one)
===============================================================================
Run each of these from inside the my-sniper-worker folder. You'll be
prompted to paste the value after each command.

  npx wrangler secret put SUPABASE_URL
  npx wrangler secret put SUPABASE_ANON_KEY
  npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY
  npx wrangler secret put GEMINI_API_KEY

NEW — required for the postback security fix in Step 1:
  npx wrangler secret put POSTBACK_SECRET
  (Paste any long random string you generate yourself — e.g. run
   `openssl rand -hex 24` in your terminal and use that output. Save this
   value somewhere safe; you'll need it again in Step 7.)

Only needed once you do the Stripe setup in Step 6 (can come back to this):
  npx wrangler secret put STRIPE_SECRET_KEY
  npx wrangler secret put STRIPE_WEBHOOK_SECRET
  npx wrangler secret put STRIPE_PRO_PRICE_ID

Only needed once you connect an affiliate network (can come back to this):
  npx wrangler secret put OFFER_FEED_URL
  npx wrangler secret put OFFER_FEED_API_KEY

✅ VERIFY: Run `npx wrangler secret list` — you should see SUPABASE_URL,
SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, GEMINI_API_KEY, and
POSTBACK_SECRET at minimum.


===============================================================================
STEP 3 — DATABASE SCHEMA FIXES
===============================================================================
Three tables your worker.js code depends on (interview_sessions,
interview_answers, sector_trends) were never created. Earnings have also
been silently returning empty for every user due to a missing security
policy.

1. Go to Supabase Dashboard > SQL Editor > New query.
2. Paste in the ENTIRE "SCHEMA.SQL ADDITIONS" SQL block given to you in the
   chat response.
3. Click Run.

✅ VERIFY:
   a) Supabase Dashboard > Table Editor — confirm you now see three new
      tables: interview_sessions, interview_answers, sector_trends.
   b) Run this in SQL Editor to confirm the missing earnings policy is now
      in place:
        SELECT policyname FROM pg_policies WHERE tablename = 'affiliate_logs';
      You should see "Users can view their own affiliate logs" in the
      results (alongside any others).
   c) Run this to confirm the new atomic withdrawal function exists:
        SELECT proname FROM pg_proc WHERE proname = 'process_withdrawal_v2';
      Should return one row.
   d) Run this to confirm the Stripe column was added:
        SELECT column_name FROM information_schema.columns
        WHERE table_name = 'profiles' AND column_name = 'stripe_customer_id';
      Should return one row.


===============================================================================
STEP 4 — FRONTEND: APPLY THE 13 MISSING JAVASCRIPT FUNCTIONS
===============================================================================
Thirteen buttons in index.html call JavaScript functions that don't exist
anywhere in the file — clicking them either does nothing or throws a
console error. This includes the Withdraw button, the KYC submit button,
the "Pay & Upgrade" button, and the One-Click AI Apply feature (which also
had no trigger button at all in the job feed).

1. Open index.html in your editor.
2. Find the line:  /* ─── RESTORE SESSION ON LOAD ─── */
3. Paste in ALL SIX blocks from "INDEX.HTML PATCHES" given to you in the
   chat response, directly ABOVE that line:
     - Withdraw functions
     - KYC functions
     - Upgrade / payment functions
     - AI Apply functions
     - Add Project functions
     - CTA email capture function
4. Find the job card template inside the renderJobs() function (search for
   `pinJob('${job.id}')`) and add the AI Apply button snippet right after
   the existing pin button, inside the same `<div style="display:flex;
   gap:8px...">` action row.
5. Save the file, then push:
     git add index.html
     git commit -m "fix: wire up withdraw, KYC, upgrade, and AI apply buttons"
     git push origin main
   (GitHub Pages updates automatically within ~60 seconds.)

✅ VERIFY — go through EVERY one of these on your live site after signing in:
   a) Dashboard > Side Task tab > click "Withdraw funds" → modal opens
      showing your real balance. Click the X → it closes. (If your balance
      is $0, that's expected on a fresh account — the modal opening/closing
      correctly is what you're checking here.)
   b) Try submitting a withdrawal for more than your balance → you should
      see "Amount exceeds your available balance," not a silent failure.
   c) Dashboard > click "Verify Identity" → upload any JPG/PNG → you should
      see a live image preview appear → click "Submit for AI Verification"
      → you should see a toast (success or "could not verify," either is
      fine for this test) and get returned to the dashboard.
   d) Landing page (logged out) > Pricing section > click "Start 7-day free
      trial" → should take you to signup if you're not logged in.
   e) Job Feed tab > look at any job card → you should now see a sparkle/AI
      icon button next to the pin button. Click it as a FREE-tier test
      account → you should be redirected to the Upgrade page with a toast
      saying it's a Pro feature (this confirms the tier gate works before
      you test the Pro path in Step 8).
   f) Scroll to the bottom CTA "Get the job you want" section > type a test
      email > click "Get alerts free" → should jump to the signup page with
      that email pre-filled in the email field.
   g) Dashboard > Projects tab > click "+ Add project" → fill in a name and
      client > Save → a new project card should appear in the list
      immediately (reminder: this one resets on page reload — see the
      roadmap note in the PRD if you want this saved permanently later).


===============================================================================
STEP 5 — AFFILIATE / SIDE TASK NETWORK (only if not already connected)
===============================================================================
The Side Task feature is fully built but needs a real affiliate network
connected to show real offers. If you haven't signed up with one yet:

1. Apply to an affiliate network that supports postback tracking, e.g.
   CPALead (cpalead.com) or OfferToro (offertoro.com).
2. Once approved, find their "API Feed URL" / "Content Locker Feed" and
   your API key in their dashboard.
3. Set them as Worker secrets (see Step 2):
     npx wrangler secret put OFFER_FEED_URL
     npx wrangler secret put OFFER_FEED_API_KEY
4. In that same network's dashboard, find the "Global Postback URL" /
   "Server-to-server Postback" setting and set it to:

     https://my-sniper-worker.626gl1ch.workers.dev/api/postback?subid={subid}&payout={payout}&country={country}&click_ip={ip}&provider=NETWORK_NAME&secret=YOUR_POSTBACK_SECRET

   Replace YOUR_POSTBACK_SECRET with the exact value you generated and
   saved in Step 2. Replace NETWORK_NAME with the network's name (e.g.
   "cpalead"). Most networks support {subid}, {payout}, {country}, {ip} (or
   similarly named) as placeholder macros — check their documentation for
   the exact macro names and substitute accordingly.

   This `secret` parameter is what stops any logged-in user from calling
   your own postback URL with their own account ID to credit themselves
   free money — only requests carrying the correct secret are accepted now.

✅ VERIFY:
   a) Dashboard > Side Task tab — you should see real offers instead of
      "No affiliate network connected yet."
   b) In the affiliate network's dashboard, send a TEST postback (most
      networks have a "send test postback" button) and confirm in Supabase
      Table Editor > affiliate_logs that a new row appeared.
   c) Try calling the postback URL yourself WITHOUT the &secret= parameter
      (or with a wrong value) — you should get "Unauthorized," confirming
      the fix is live and the endpoint can't be abused.


===============================================================================
STEP 6 — REAL PAYMENTS: STRIPE SETUP
===============================================================================
Your project already has a complete, ready-to-use Stripe integration guide
sitting in PAYMENT_SETUP_DIRECTIONS.txt in your project folder — it was
written specifically for your stack and already contains the exact code for
the checkout route and the secure webhook (the same code you applied in
Step 1's worker.js patches).

Open that file now and follow it top to bottom: it walks through creating
your Stripe account, creating the $9/mo product, getting your API keys,
registering the webhook, and testing with Stripe's fake test card. The
worker.js code it describes is already inside your file from Step 1 — you
only need to do the Stripe DASHBOARD steps now (account, product, keys,
webhook registration) and set the three STRIPE_* secrets from Step 2.

✅ VERIFY (this is also Section 8 of PAYMENT_SETUP_DIRECTIONS.txt):
   a) Click "Pay $9.00 & Upgrade" on a test account with Stripe card
      selected → you should be redirected to an actual Stripe-hosted
      checkout page (not an error).
   b) Pay with Stripe's test card 4242 4242 4242 4242, any future expiry,
      any CVC.
   c) After completing checkout, you should land back on your site.
   d) Supabase Table Editor > profiles > find your test user > confirm
      current_tier is now "paid".
   e) Stripe Dashboard > Developers > Webhooks > your endpoint → confirm a
      200 response is logged for checkout.session.completed.


===============================================================================
STEP 7 — FULL REGRESSION TEST (run this after Steps 1–6 are all done)
===============================================================================
Go through this checklist top to bottom on your LIVE site with a fresh test
account. This is the same kind of pass that found the original bugs — don't
skip steps even if you're confident they'll pass.

SIGNUP & ONBOARDING
[ ] Sign up with a new email — account created, no "Database error saving
    new user" message.
[ ] Paste a short bio in the resume-scan step and click "Scan with AI" —
    fields auto-fill within a few seconds.
[ ] Pick 3 sectors, complete signup, land on the dashboard.

JOB FEED
[ ] Dashboard loads with a real job count (not stuck on "Scanning live
    sources...").
[ ] Switching sector filters changes the job list.
[ ] Clicking the bookmark icon on a job shows "Job pinned."
[ ] Clicking "Sort: Best match" shows match-score badges on job cards.

CAREER PREP
[ ] Resume tab > "Score my resume" returns a number 0–100 and tips.
[ ] Sector Trends panel shows trending skills (first load may take a few
    extra seconds while it generates and caches — that's expected).
[ ] Interview Prep > "Start practice session" shows 3 questions (free
    account) and lets you submit an answer.

SIDE TASK
[ ] Side Task tab shows either real offers or "No affiliate network
    connected yet" (not an error).
[ ] "Recent earnings" shows real rows if you've run a test postback (Step
    5b), not a permanently empty list.
[ ] Withdraw modal opens, validates the minimum ($2) and balance correctly.

IDENTITY & MONEY
[ ] KYC page accepts an image upload, shows a preview, and submits without
    a console error.
[ ] Submitting a withdrawal larger than your balance is rejected with a
    clear message, not a silent failure or a negative balance.
[ ] (Pro account) Submitting a valid withdrawal succeeds and your displayed
    balance drops by the correct amount immediately.

MONETIZATION
[ ] Free account sees the ad banner; it disappears immediately after
    upgrading to Pro.
[ ] Stripe checkout flow completes end-to-end (Step 6).
[ ] AI Apply button is visible on every job card; free accounts are
    redirected to Upgrade; Pro accounts see a generated proposal within a
    few seconds.

SECURITY SPOT-CHECKS (do these from a terminal, not the browser)
[ ] curl -X POST .../api/payment/webhook with a fake JSON body and NO
    stripe-signature header → should be rejected with "Invalid signature,"
    NOT silently grant Pro access.
[ ] curl -X POST ".../api/postback?subid=YOUR_OWN_USER_ID&payout=999&country=XX"
    WITHOUT the &secret= param → should return "Unauthorized."


===============================================================================
APPENDIX A — FIRST LIVE SIGNUP TEST: WHAT BROKE AND WHY
===============================================================================
After deploying the marketing site but before finishing Step 1 of this
guide, a real signup attempt surfaced three separate issues worth
distinguishing clearly:

1. WORKER NOT RESOLVING (net::ERR_NAME_NOT_RESOLVED on /api/profile)
   This is a DNS-level failure, not a 404 — it means the worker named
   my-sniper-worker doesn't currently exist under your Cloudflare account
   at all (or its workers.dev route is disabled). This is why the dashboard
   fails to load right after signup even though the Supabase account itself
   gets created fine (plain email signup talks to Supabase directly, not
   your worker — it's only the very next step, loading the profile, that
   needs the worker).
   FIX: Complete Step 1 of this guide (deploy my-sniper-worker for real),
   then run:
     curl -I https://my-sniper-worker.626gl1ch.workers.dev/debug/env
   If curl can't resolve the host at all, go to Cloudflare Dashboard >
   Workers & Pages and confirm "my-sniper-worker" is actually listed. If
   it's listed, open it > Settings > Domains & Routes and confirm the
   workers.dev route is enabled.

2. SOCIAL LOGIN BUTTONS (Google/Facebook/LinkedIn/Instagram all failing
   with "Unsupported provider: provider is not enabled")
   This is NOT a code bug — handleOAuth() in index.html is correct. None of
   these four providers have been turned on yet in Supabase.
   FIX, per provider, in Supabase Dashboard > Authentication > Providers:
     a. Create OAuth credentials in that platform's own developer console
        (Google Cloud Console / Facebook for Developers / LinkedIn Developer
        Portal — Instagram piggybacks on the Facebook app).
     b. Use the callback URL Supabase shows on that same Providers page as
        the redirect URI when registering the app on the provider's side.
     c. Paste the resulting Client ID + Client Secret into Supabase and
        toggle the provider on.
   ALSO REQUIRED (separate from the above, applies to all providers at
   once): Authentication > URL Configuration > both Site URL and Redirect
   URLs must include exactly:
     https://626gl1ch.github.io/JOB-FINDER-SAAS-WEB-APP/
   (trailing slash included) — a mismatch here causes a different failure
   even after the provider itself is enabled correctly.
   Recommendation: get Google working first end-to-end, then repeat for the
   others. If you don't need Instagram sign-in soon, it's fine to remove
   that button rather than configure all four before launch.

3. MISSING HERO IMAGE (hero-bg.jpg 404)
   index.html references assets/hero-bg.jpg, but no assets/ folder exists
   in the repo. Either add a real image at that exact path, or remove that
   <img> tag if you don't have one ready — purely cosmetic, not blocking.

EXTENSION NOISE YOU CAN IGNORE
   Console lines from "contentscript.js" mentioning ObjectMultiplex,
   MaxListenersExceededWarning, or "Could not establish connection" are
   from a browser extension (most likely a crypto wallet like MetaMask)
   injecting itself into every page you visit — unrelated to SnipeJob.
   Confirm by testing in an Incognito window with extensions disabled.


===============================================================================
APPENDIX B — TROUBLESHOOTING
===============================================================================
"debug/env shows false for one of the keys"
  → You ran the wrangler secret put command in the wrong folder. cd into
    my-sniper-worker first, then re-run the command for that specific key.

"Routes still say Not Found after deploying"
  → You're deploying the wrong file. Confirm my-sniper-worker/wrangler.jsonc
    has "main": "src/index.js" and that you ran `npx wrangler deploy` from
    INSIDE the my-sniper-worker folder, not the project root.

"Database error saving new user" during signup
  → Re-run the ORIGINAL schema.sql (not just the additions from Step 3) in
    Supabase SQL Editor — this re-applies the handle_new_user() trigger fix.

"AI features return a 502 / temporarily unavailable"
  → Your GEMINI_API_KEY secret is missing, wrong, or you've hit a Gemini
    quota limit. Check Cloudflare Dashboard > Workers > my-sniper-worker >
    Logs (Real-time Logs) while reproducing the error to see the exact
    Gemini error message.

"Trends panel is empty even after Step 3"
  → The first request for a sector triggers Gemini generation, which takes
    a few seconds — wait and refresh. If still empty after that, check the
    real-time Worker logs for a Gemini error.

"Earnings still show empty after Step 3"
  → Double-check the SQL in Step 3 actually ran without error (Supabase SQL
    Editor shows a green "Success" message). Re-run the policy check query
    from Step 3's verification.

===============================================================================
END OF GUIDE
===============================================================================
